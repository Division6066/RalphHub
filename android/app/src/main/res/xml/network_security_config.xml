<?xml version="1.0" encoding="utf-8"?>
<!--
    RalphHub Mobile Network Security Config
    Allows cleartext HTTP to LAN addresses (192.168.x.x, 10.x.x.x)
    for local desktop sync. All external traffic remains HTTPS.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <!-- Allow cleartext HTTP for local LAN sync with desktop -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <!-- 192.168.x.x range for home/office LAN -->
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">192.168.1.0</domain>
        <!-- 10.x.x.x range for corporate/VPN LAN -->
        <domain includeSubdomains="true">10.0.0.0</domain>
    </domain-config>
</network-security-config>
